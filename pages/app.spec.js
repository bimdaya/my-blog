import React from 'react';
import { render } from '@testing-library/react';

import getInitialProps from './_app';
import getAllPostContent from '../lib/getPosts';

//jest.mock('getAllPostContent', () => jest.fn());

describe('getInitialProps', () => {
  it('Returns ', async () => {});
});
